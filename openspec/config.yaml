# OpenSpec configuration — Polla Mundialista 2026
# Auto-generated by sdd-init (2026-06-14)

mode: hybrid
project: ProyectoPollaMundiailista

context: |
  Polla Mundialista 2026 — FIFA World Cup 2026 prediction pool webapp.
  Python 3.13 scripts in scripts/: calificar.py (scoring engine with FIFA-compliant
  H2H tiebreakers), generar_sitio.py (static HTML generator with leaderboard and
  admin modal), fetch_resultados.py (API fetcher from worldcup26.ir), importar_excel*.py
  (3 parser variants for different Excel layouts). Static HTML output in docs/
  (index.html public, admin.html admin panel). No frameworks, no build tools.
  JSON data layer: data/pollas/*.json, data/puntajes.json, data/participantes.json,
  data/resultados_reales.json. Pipeline: actualizar.sh (fetch → score → generate → deploy
  via git push to GitHub Pages). Python stdlib only plus openpyxl for Excel parsing.
  Multi-polla support (same participant can submit multiple entries).

strict_tdd: false

testing:
  runner: null
  reason: "No test files, no pytest.ini/pyproject.toml, no test runner configured. All scripts are CLI tools invoked directly."
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false

phases:
  proposal:
    required: true
  spec:
    required: true
  design:
    required: true
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
